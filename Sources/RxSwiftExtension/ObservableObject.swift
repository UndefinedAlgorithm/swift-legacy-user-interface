/*
import RxSwift

public protocol ObservableObject: AnyObject {
  // Make sure that the ObjectWillChangeObservable never fails, however
  // we cannot guarantee this using RxSwift yet.
  associatedtype ObjectWillChangeObservable: ObservableType
    = ObservableObjectPublisher
  var objectWillChange: ObjectWillChangeObservable { get }
}
*/
