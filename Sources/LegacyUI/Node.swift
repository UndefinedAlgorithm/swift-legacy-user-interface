public protocol Node {
  // Right now this protocol is a no-op, but in the future
  // we want to make sure that we also support no only
  // 'environment' for data flow downwards, but also
  // 'preferences' for data flow upwards.
}
